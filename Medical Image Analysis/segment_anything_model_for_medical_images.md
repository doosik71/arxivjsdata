# Segment Anything Model for Medical Images?

Yuhao Huang, Xin Yang, Lian Liu, Han Zhou, Ao Chang, Xinrui Zhou, Rusi Chen, Junxuan Yu, Jiongquan Chen, Chaoyu Chen, Sijing Liu, Haozhe Chi, Xindi Hu, Kejuan Yue, Lei Li, Vicente Grau, Deng-Ping Fan, Fajin Dong, Dong Ni

## 🧩 Problem to Solve

Segment Anything Model (SAM)은 자연 이미지 분할에서 탁월한 성능을 보인 최초의 파운데이션 모델이지만, 의료 이미지 분할(MIS)은 복잡한 양상, 미세한 해부학적 구조, 불확실하고 복잡한 객체 경계, 광범위한 객체 크기 등으로 인해 더욱 도전적입니다. 기존 연구들은 제한된 데이터셋, 단일 테스트 전략, 또는 불완전한 평가로 인해 SAM의 의료 데이터 성능을 충분히 검증하지 못했습니다. 본 연구는 SAM의 MIS 성능을 종합적으로 검증하고, 의료 객체 인식 능력을 이해하며, SAM의 적절한 활용 및 개발 방향을 제시하는 것을 목표로 합니다.

## ✨ Key Contributions

- **대규모 의료 이미지 데이터셋 구축:** 18개 양상(modality), 84개 객체, 125개 객체-양상 쌍, 1050K 2D 이미지, 6033K 마스크를 포함하는 대규모 의료 분할 데이터셋인 COSMOS 1050K를 구축했습니다.
- **종합적인 SAM 성능 분석:** COSMOS 1050K 데이터셋에서 다양한 SAM 모델(ViT-B, ViT-H)과 전략(자동 및 수동 프롬프트)을 포괄적으로 분석했습니다.
- **주요 연구 결과 도출:**
  1. SAM은 특정 객체에서는 놀라운 성능을 보였으나, 다른 상황에서는 불안정하거나 불완전하며, 심지어 완전히 실패하는 경우도 있었습니다.
  2. 더 큰 ViT-H 모델을 사용한 SAM이 작은 ViT-B 모델보다 전반적으로 더 나은 성능을 보였습니다.
  3. 수동 힌트, 특히 박스(box) 프롬프트가 "Everything" 모드보다 더 나은 성능을 이끌어냈습니다.
  4. SAM은 높은 라벨링 품질과 적은 시간으로 인간의 어노테이션을 돕는 데 효과적이었습니다.
  5. SAM은 중심점 및 타이트 박스 프롬프트의 무작위성에 민감하며, 심각한 성능 저하를 겪을 수 있습니다.
  6. SAM은 한두 개의 포인트로는 대화형 방법보다 우수했지만, 포인트 수가 증가할수록 성능이 뒤처졌습니다.
  7. SAM의 성능은 경계 복잡성, 강도 차이 등 다양한 요인과 관련이 있었습니다.
  8. 특정 의료 작업에 SAM을 파인튜닝하면 평균 DICE 성능이 ViT-B의 경우 4.39%, ViT-H의 경우 6.68% 향상되었습니다.
- **코드 및 모델 공개:** 연구 결과의 재현성을 위해 코드와 모델을 공개했습니다.

## 📎 Related Works

- **파운데이션 모델의 등장:** ChatGPT 및 GPT-4와 같은 대규모 언어 모델의 발전과 CLIP, ALIGN, DALL·E와 같은 컴퓨터 비전 모델이 SAM의 개발에 영감을 주었습니다. 하지만 이들은 이미지 분할, 특히 의료 이미지 분할에 최적화되지 않았습니다.
- **SAM의 소개:** Kirillov et al. (2023)이 제안한 SAM은 ViT 기반으로 1100만 이미지와 10억 개의 마스크로 학습되었으며, 제로샷 분할 성능이 특징입니다.
- **비의료 이미지에서의 SAM:**
  - 위장된 객체(camouflaged objects) (Tang et al., 2023; Ji et al., 2023a) 및 산업 현장의 결함 감지에서는 낮은 성능을 보였습니다.
  - Ji et al. (2023b)은 자연 이미지, 농업, 제조, 원격 감지 등 다양한 분야에서 SAM을 평가했으며, 수동 프롬프트가 자동 "Everything" 방식보다 결과 개선에 효과적임을 입증했습니다.
- **의료 이미지 분석에서의 SAM (선행 연구):**
  - Ji et al. (2023a)은 "Everything" 모드에서 병변 분할에 SAM을 적용하여 경계가 명확한 기관에는 능숙하지만, 불분명한 병변에는 어려움을 겪음을 발견했습니다.
  - Mohapatra et al. (2023)은 뇌 추출 작업에서 SAM이 기존 도구보다 우수함을 보였습니다.
  - Deng et al. (2023)은 디지털 병리 이미지에서 SAM이 크고 연결된 객체에는 뛰어났으나, 밀집된 인스턴스 분할에는 만족스럽지 못함을 지적했습니다.
  - Zhou et al. (2023)은 용종(polyp) 분할에서 SAM과 최신 방법 간의 큰 격차를 확인했습니다.
  - 최근 여러 연구(He et al., 2023; Mazurowski et al., 2023; Ma and Wang, 2023; Wu et al., 2023)에서 SAM의 의료 이미지 성능을 평가하며, 원본 SAM의 한계와 파인튜닝을 통한 개선 가능성을 제시했습니다.

## 🛠️ Methodology

1. **COSMOS 1050K 데이터셋 구축:**
   - 53개의 공개 데이터셋을 수집하고 표준화하여 1,050,311개의 2D 이미지/슬라이스와 6,033,198개의 마스크를 포함하는 대규모 데이터셋을 구성했습니다.
   - CT, MRI, 초음파(US), X-ray, 조직병리(Histopathology) 등 18가지 양상과 뇌종양, 폐, 심장, 세포 등 84가지 객체(총 125가지 객체-양상 쌍)를 포함합니다.
   - **제외 기준:** 극도로 작은 객체, 2D 슬라이스에서 크게 분리되는 3D 볼륨 객체, 상대적으로 이산적인 전체 구조를 가진 객체들을 제외하여 프롬프트 모드 테스트의 유효성을 확보했습니다.
   - **데이터 전처리:** 3D 볼륨은 해상도가 높은 주 시야 평면을 따라 슬라이스를 추출하고, 라벨 픽셀 합이 50보다 큰 슬라이스만 유지했습니다. 모든 이미지 강도는 $0$에서 $255$ 범위로 min-max 정규화했으며, 이미지와 라벨은 PNG 형식으로 저장했습니다.
2. **SAM 모델 개요:**
   - **이미지 인코더:** MAE(Masked Autoencoder) 기술로 사전 학습된 ViT(Vision Transformer) 백본을 사용합니다.
   - **프롬프트 인코더:** 마스크 프롬프트는 CNN, 포인트 및 박스 프롬프트는 위치 인코딩, 텍스트 프롬프트는 CLIP으로 인코딩합니다 (텍스트 프롬프트는 공식적으로 아직 출시되지 않음).
   - **마스크 디코더:** 모든 임베딩을 디코딩하여 마스크를 예측합니다.
3. **테스트 모델 및 전략:**
   - **모델:** SAM의 ViT-B (91M 파라미터)와 ViT-H (636M 파라미터) 인코더를 사용했습니다.
   - **전략:**
     - $S_1$: 자동 "Everything" 모드 (수동 힌트 없이 이미지 내 모든 객체 분할).
     - $S_2$: 하나의 양성 포인트.
     - $S_3$: 다섯 개의 양성 포인트.
     - $S_4$: 다섯 개의 양성 포인트와 다섯 개의 음성 포인트.
     - $S_5$: 하나의 박스.
     - $S_6$: 하나의 박스와 하나의 양성 포인트.
   - **프롬프트 선택 규칙:** 양성 포인트는 GT 마스크의 질량 중심과 균일 샘플링으로, 음성 포인트는 GT 바운딩 박스를 2배 확장한 비-GT 영역에서 균일 샘플링으로 선택했습니다. 박스 프롬프트는 GT 마스크의 바운딩 박스를 사용했습니다.
4. **추론 효율성 최적화:** 이미지 임베딩은 한 번만 계산하여 `.npy` 파일로 저장하고, 여러 테스트 전략에서 재사용하여 추론 시간을 약 $n$배 단축했습니다.
5. **마스크 매칭 메커니즘:** SAM이 생성한 여러 바이너리 마스크 중 GT와 DICE 점수가 가장 높은 마스크를 선택하여 성능을 평가했습니다.
6. **평가 지표:** DICE Coefficient (DICE), Jaccard Similarity Coefficient (JAC), Hausdorff Distance (HD)를 사용했습니다.
7. **태스크별 파인튜닝:** 45개의 일반적인 객체에 대해 SAM의 마스크 디코더만 파인튜닝했으며, 이미지 인코더와 프롬프트 인코더는 고정했습니다.

## 📊 Results

- **모델 크기별 성능:** ViT-H는 ViT-B에 비해 전반적인 성능이 우수했으며 (예: $S_1$에서 DICE 7.47%p, HD 10.61픽셀 향상), 더 안정적인 성능을 보였습니다.
- **테스트 모드별 성능:**
  - "Everything" 모드($S_1$)는 가장 낮은 성능을 기록했습니다.
  - 포인트 프롬프트($S_2$-$S_4$)의 경우, 포인트 수가 증가할수록 성능이 꾸준히 향상되었습니다.
  - 박스 프롬프트($S_5$)는 가장 좋은 성능을 보였고, 여기에 추가 포인트를 포함해도($S_6$) 큰 변화는 없었습니다. 박스 프롬프트가 포인트보다 더 중요한 영역 정보를 제공함을 시사합니다.
- **양상별 성능:** X-ray, 조직병리, 대장내시경, 확산강조영상(DW MRI) 양상에서 평균 DICE 성능이 90% 이상으로 가장 높고 안정적이었습니다. 나머지 양상에서는 성능이 낮고 불안정한 경향을 보였습니다.
- **추론 시간 분석:** ViT-H의 임베딩 생성 시간은 ViT-B의 약 4배였습니다. "Everything" 모드($S_1$)는 수백 개의 포인트를 처리하고 NMS와 같은 후처리가 필요하여 가장 많은 시간이 소요되었습니다. 반면, 수동 프롬프트 모드($S_2$-$S_6$)의 프롬프트 인코딩 및 마스크 디코딩은 0.01초 미만으로 실시간 사용에 적합했습니다.
- **"Everything" 모드에서의 포인트 수:** 포인트 격자 밀도를 높이면(8x8에서 256x256) DICE가 점진적으로 증가하지만, 객체 무결성을 손상시키고 테스트 시간을 크게 늘릴 수 있어 성능과 효율성 간의 균형이 중요합니다.
- **분할 결과 상관 요인:**
  - DICE 점수는 **푸리에 차수(경계 복잡성)** 및 **전경-배경 강도 차이**와 중간 정도의 상관관계($0.4 \le \rho < 0.7$)를 보였습니다. 경계가 복잡하거나 대비가 낮은 객체에서 SAM의 성능이 저조했습니다.
  - **객체 크기**와는 약한 상관관계($0.2 \le \rho < 0.4$)를 가졌습니다.
  - **양상**과 **종횡비**와는 상관관계가 없었습니다.
- **어노테이션 시간 및 품질 분석:** SAM의 도움을 받으면 인간 어노테이션의 품질이 향상되고(HCE 지수 0.27 감소), 어노테이션 속도가 약 25% 빨라지는 것으로 나타났습니다(이미지당 약 1.31분 절약).
- **프롬프트 무작위성의 영향:** SAM은 중심점 및 바운딩 박스 프롬프트의 무작위성에 매우 민감했습니다. 20-30 픽셀의 이동에서 단일 포인트($S_2$)는 14.62%, 박스($S_5$)는 24.11%, 박스+포인트($S_6$)는 29.93%의 DICE 성능 저하를 보였습니다. 여러 포인트를 사용하면 저하가 완화되었습니다.
- **대화형 분할 방법과의 비교:** SAM은 단일 포인트의 첫 번째 상호작용에서 FocalClick, SimpleClick보다 우수했습니다. 하지만 반복 횟수(포인트 수)가 증가할수록 SAM의 성능은 느리게 증가하거나 심지어 감소하는 반면, 기존 대화형 방법은 꾸준히 향상되어 10개 이상의 포인트에서는 SAM을 앞질렀습니다.
- **태스크별 파인튜닝:** COSMOS 1050K의 일부 데이터를 사용하여 SAM의 마스크 디코더를 파인튜닝한 결과, 평균 DICE 성능이 ViT-B는 4.39%p, ViT-H는 6.68%p 향상되었습니다. 이는 ViT-H의 강력한 학습 능력을 보여주지만, 특정 객체(예: 수가 적거나 RGB 컬러 코딩)에서는 성능이 저하되어 신중한 파인튜닝 설계가 필요함을 시사합니다.

## 🧠 Insights & Discussion

- **SAM의 잠재력과 한계:** SAM은 범용 MIS 모델이 될 잠재력이 있지만, 현재 의료 이미지 분할 작업에서는 성능이 안정적이지 않습니다. SAM의 학습 데이터셋(SA-1B)이 자연 이미지에 치중되어 있어, 회색조, 불분명한 경계, 유사한 전경/배경, 광범위한 객체 크기 등 의료 이미지의 특성을 잘 반영하지 못하는 것이 주요 원인입니다.
- **향후 연구 방향:**
  - **의료 SAM의 의미론적 인식:** 현재 SAM은 객체를 인식하지만 분류는 하지 못합니다. CLIP 또는 Grounding DINO와 같은 Open-Vocabulary Object Detection (OVOD) 모델과의 결합을 통해 의료 객체의 의미론적 분류 능력을 갖춘 SAM을 개발하는 것이 흥미로운 방향입니다. 이는 의료 객체의 다양성과 복잡성으로 인해 도전적입니다.
  - **SAM 대 기존 분할 방법:** 제한된 의료 데이터로 파인튜닝된 SAM (2D/3D 어댑터를 사용한 Med-SAM 또는 MA-SAM)이 기존의 태스크별 SOTA 방법(예: nnUnet)보다 뛰어남이 입증되었습니다. 이는 의료 이미지 분할 커뮤니티가 처음부터 모델을 훈련하는 대신 파운데이션 모델을 파인튜닝하는 방향으로 나아갈 수 있음을 시사합니다.
  - **2D SAM 또는 3D SAM:** 의료 데이터는 2D/3D/4D 등 다양한 양상을 가지므로, 모든 유형의 데이터를 일관되게 처리할 수 있는 2D 모델을 구축하는 것이 더 실용적입니다. 제한된 3D 데이터는 3D 파운데이션 모델 훈련을 어렵게 하며, 고품질 3D 데이터 합성이 한계를 극복하는 방법이 될 수 있습니다.
  - **대규모 의료 어노테이션 가속화:** SAM은 전문가의 수동 어노테이션에 필요한 시간과 노력을 획기적으로 줄여줄 수 있습니다 (예: 8,448개 CT 볼륨 어노테이션 시간을 30.8년에서 3주로 단축). 프롬프트 기반 분할 기능은 유연성을 제공하며, 단일 SAM 네트워크가 다중 양상, 다중 클래스 의료 데이터셋의 어노테이션을 지원하여 라벨링 소프트웨어(MONAI Label, Pair annotation)의 경량화 및 효율적인 배포에 기여할 수 있습니다.
- **현재의 약점:** SAM은 프롬프트 노이즈에 대한 견고성과 다중 라운드 상호작용 능력에서 약점을 보였습니다. 향후 SAM 훈련 시 반복 학습 전략을 최적화하거나, 파인튜닝을 통해 이러한 능력을 강화해야 할 것입니다.

## 📌 TL;DR

**문제:** Segment Anything Model (SAM)이 자연 이미지에서 뛰어난 성능을 보였지만, 복잡한 의료 이미지 분할(MIS) 작업에서의 잠재력과 한계는 불확실했습니다. 기존 연구들은 제한적인 데이터셋과 평가 방식으로 인해 종합적인 검증이 부족했습니다.

**제안 방법:** 본 연구는 18개 양상, 84개 객체, 1050K 2D 이미지 및 6033K 마스크로 구성된 대규모 의료 데이터셋인 COSMOS 1050K를 구축했습니다. 이 데이터셋을 활용하여 SAM의 ViT-B 및 ViT-H 모델을 자동("Everything" 모드) 및 다양한 수동 프롬프트(포인트, 박스) 전략 하에서 종합적으로 평가했습니다. 또한, SAM 성능에 영향을 미치는 경계 복잡성, 강도 차이 등 객체 특성과의 상관관계를 분석하고, 어노테이션 효율성, 프롬프트 무작위성, 기존 대화형 분할 방법과의 비교, 그리고 MIS 특정 태스크를 위한 파인튜닝의 효과를 검증했습니다.

**핵심 결과:** SAM은 일부 의료 객체에서 주목할 만한 성능을 보였으나, 전반적으로 불안정하거나 실패하는 경우가 있었고, ViT-H가 ViT-B보다 우수했습니다. "Everything" 모드보다는 수동 힌트, 특히 박스 프롬프트가 훨씬 더 나은 결과를 제공했습니다. SAM은 인간 어노테이션의 품질을 높이고 시간을 단축하는 데 효과적이었지만, 프롬프트의 무작위성에 민감하여 성능이 크게 저하될 수 있으며, 여러 포인트를 사용한 다중 라운드 상호작용에서는 기존 대화형 분할 방법보다 성능이 뒤처졌습니다. SAM의 성능은 객체의 경계 복잡성 및 강도 차이와 유의미한 상관관계를 보였고, 의료 데이터로 마스크 디코더만 파인튜닝했을 때 평균 DICE 성능이 ViT-B의 경우 4.39%p, ViT-H의 경우 6.68%p 향상되었습니다.
